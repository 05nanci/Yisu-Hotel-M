# PostgreSQL 数据库配置（仅改DB_PASSWORD！）
DB_HOST=127.0.0.1   # 本地数据库，固定值
DB_PORT=5432        # PostgreSQL默认端口，固定值
DB_NAME=yisu_hotel 
DB_USER=postgres    # PostgreSQL默认管理员账号，固定值
DB_PASSWORD=123456  

# 后端服务配置
PORT=10086  # 后端端口，和前端代理的target一致（http://localhost:10086）